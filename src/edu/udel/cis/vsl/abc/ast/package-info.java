/**
 * The ast module, providing an Abstract Syntax Tree
 * representation of a program.  This module is large
 * and is therefore divided into several submodules.
 * Each submodule has its own interface.  This package proper contains
 * the module entry point, {@link edu.udel.cis.vsl.abc.ast.ASTs}
 * and an exception class specific to ASTs.
 */
package edu.udel.cis.vsl.abc.ast;

