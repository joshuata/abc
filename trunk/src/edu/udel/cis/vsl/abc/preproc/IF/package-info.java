/**
 * <p>Module <strong>preproc</strong> is used to preprocess a
 * file or character stream.  It tokenizes the stream, then interprets
 * the preprocessor directives occurring in that stream and applies them
 * to create a "post-preprocessing" stream.    This last stream is typically
 * used as input to the parser.</p>
 */
package edu.udel.cis.vsl.abc.preproc.IF;