#include <civlc.h>

// void* $malloc($heap *h, int size);

void main() {
  //$heap h;
  $scope s = $here;
  double *p = (double *) $malloc(s, 5*sizeof(double));
  double *z = (double *) $malloc($root,  5*sizeof(double));
  
  free(p);
  free(z);
}   
